name: Docker Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: nongrei
        password: dckr_pat_FL_Bx1aFz3fM4cBTs7Np5mtvJdk
    
    - name: Build and Push
      uses: docker/build-push-action@v3
      with:
        context: .
        push: true
        tags: nongrei/nongreid.kohya-sd-webui:latest
